# DataJoint config & secrets — never commit
datajoint.json
.secrets/

# Virtual environments
.venv/
venv/
env/

# uv lockfile — this is a library (submoduled into host envs), not an app;
# deps resolve against the host, so we don't pin a lockfile.
uv.lock

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/
*.egg

# Testing / coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
